# Laisser vide en dev local : le proxy Vite (vite.config.js) redirige /api
# vers http://localhost:3001 automatiquement.
#
# En prod (Vercel), renseigner l'URL complete du backend deploye sur Render,
# terminee par /api :
# VITE_API_URL=https://deathchallenge-backend.onrender.com/api
VITE_API_URL=
